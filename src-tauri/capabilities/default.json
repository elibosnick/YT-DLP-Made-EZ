{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions this app needs. Tauri v2 denies everything not listed here, so this file is the security boundary — do not add permissions speculatively.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "dialog:default",
    "opener:allow-reveal-item-in-dir"
  ]
}
